[project]
name = "django-girls-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dario curreri", email = "dariocurr@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django~=5.2.12",
]

[project.scripts]
django-girls-tutorial = "django_girls_tutorial:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
